# ==============================================================================
# 汐储 TideShift · 开发 / 测试依赖
# ------------------------------------------------------------------------------
# 安装：pip install -r requirements-dev.txt
# 本文件在运行依赖之上追加测试工具链，因此「测试全通过」可被任何人复现。
# 约束含义同 requirements.txt：下界 = 实测版本，上界 = 下一个可能不兼容的版本。
# ==============================================================================

-r requirements.txt

pytest>=9.1.1,<10.0
coverage>=7.16.0,<8.0
httpx>=0.28.1,<1.0        # fastapi.testclient 的底层 HTTP 客户端
