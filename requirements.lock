# ==============================================================================
# 汐储 TideShift · 直接依赖精确锁定
# ------------------------------------------------------------------------------
# 用途：把「实测通过」的直接依赖版本钉死到具体版本号，避免任何版本漂移。
# 安装：pip install -r requirements.lock
#
# 说明：本文件锁定的是**直接依赖**（由验证环境的 pip freeze 结果筛选得到），
#       不含传递依赖（starlette、langsmith、tiktoken 等由 pip 按各自约束解析）。
#       需要连传递依赖也完全复现时，请在安装后自行执行 pip freeze 归档。
#
# 生成 / 实测环境：Python 3.13.14 · Windows 10
# 实测结果：快测（默认跳过 slow）70 项通过；全量（含 slow）92 项通过
#
# 维护约定
#   1. 本文件与 requirements.txt 的口径已逐条对齐（requirements.txt 的下界 = 本文件版本）；
#      改动任意一侧必须同步另一侧；
#   2. 重新生成后请跑一次 pytest，并更新上方「实测结果」；
#   3. langchain-community 来自 langchain 的 `community` extra，本项目代码未引用，
#      保留它只是为了如实记录生成环境的现状，不属于项目依赖。
# ==============================================================================

coverage==7.16.0
cryptography==49.0.0
fastapi==0.141.1
highspy==1.15.1
httpx==0.28.1
langchain==1.3.14
langchain-community==0.4.2
langchain-core==1.5.3
langchain-openai==1.4.1
langgraph==1.2.10
numpy==2.5.1
openpyxl==3.1.5
pandas==3.0.5
PuLP==3.3.2
pydantic==2.13.4
pytest==9.1.1
python-multipart==0.0.32
scikit-learn==1.9.0
scipy==1.18.1
uvicorn==0.52.1
xgboost==3.4.1
